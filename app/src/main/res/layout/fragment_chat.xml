<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:overScrollMode="never"
    tools:context=".Fragment.MainPageFragment.ChatFragment"
    android:padding="@dimen/headerTextSize"
    android:background="@color/backgroundColor">

    <!-- TODO: Update blank fragment layout -->
    <!--   <TextView
           android:layout_width="match_parent"
           android:layout_height="match_parent"
           android:text="Chat Fragment" />-->

    <WebView
        android:id="@+id/talkjs"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />

    <!--<RelativeLayout
        android:id="@+id/layout_top_chat"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:background="@drawable/radius_relativelayout">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/studioAvatar_chat"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/download"
            />

        <TextView
            android:id="@+id/nameStudio_chat"
            android:paddingStart="10dp"
            android:layout_toEndOf="@id/studioAvatar_chat"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1.5"
            android:text="Name Studio 1"
            android:textColor="#000000"
            android:textSize="24dp" />

    </RelativeLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:paddingTop="10dp"
        android:id="@+id/rcv_message"
        android:layout_width="match_parent"
        android:layout_below="@id/layout_top_chat"
        android:layout_above="@id/layout_bottom_chat"
        android:layout_height="match_parent"
        />

    <RelativeLayout
        android:id="@+id/layout_bottom_chat"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true">
        <EditText
            android:id="@+id/edit_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="18sp"
            android:layout_toStartOf="@id/btn_send"
            android:layout_marginEnd="10dp"
            android:layout_centerVertical="true"
            android:textColor="@color/GrayText2"
            android:hint="Message"/>
        <Button
            android:id="@+id/btn_send"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_alignParentEnd="true"
            android:text="send"/>

    </RelativeLayout>-->

    <!--<Button
        android:id="@+id/btnZaloPayChat"
        android:layout_above="@id/layout_bottom_chat"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="ZALO PAY"/>-->


</RelativeLayout>